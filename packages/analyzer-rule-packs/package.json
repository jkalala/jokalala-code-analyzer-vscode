{
  "name": "@jokalala/analyzer-rule-packs",
  "version": "1.0.0",
  "description": "Shared versioned SAST rule packs for cloud Stage-1 and local Tier-1 engines",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "packs"
  ],
  "scripts": {
    "compile": "tsc -p ./",
    "typecheck": "tsc -p ./ --noEmit",
    "test:parity": "node ./scripts/parity-fixtures.js",
    "test": "pnpm run compile && pnpm run test:parity"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
